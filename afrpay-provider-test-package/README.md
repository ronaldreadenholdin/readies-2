# AfrPay Provider Test Package

Open `provider-test-board.html` in a browser.

Default provider name is `AfrPay`. Change the connection names/codes if needed, then run both connection tests.

If Windows/Mac cannot open a ZIP, extract this archive with the system unzip tool or ask for the folder version.
